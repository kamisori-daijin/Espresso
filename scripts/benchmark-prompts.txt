# Fixed prompt suite for autoresearch benchmark hardening.
# Format: id:prompt_text (one per line, # lines are comments)
#
# Each prompt exercises a different input length and generation behavior.
# CoreML sequence-length bucketing (nextPowerOfTwo) means each prompt
# hits a different compute profile — this is intentional.

short:Hello
medium:The quick brown fox jumps over the lazy dog near the river
long:In a distant future where artificial intelligence has become deeply integrated into every aspect of daily life, researchers at a small university lab made an unexpected discovery
