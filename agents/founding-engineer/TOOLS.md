# Tools

- `swift build` -- compile all targets
- `swift test` -- run all tests
- `swift test --filter <pattern>` -- run specific tests
- `swift run espresso-bench` -- benchmarking CLI
- `swift run espresso-generate` -- token generation CLI
- `./espresso compare` -- Espresso vs CoreML comparison
- `./espresso doctor` -- check host readiness
